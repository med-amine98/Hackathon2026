fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 doesn't pin bcrypt, so pip would otherwise install the
# latest bcrypt (4.1+), which raises ValueError on its own internal
# 72-byte self-test instead of the silent-truncate behavior passlib 1.7.4
# was written against - crashes the first hash/verify call (i.e. first
# signup/login). Known passlib/bcrypt incompatibility; pin bcrypt<4.1.
bcrypt==4.0.1
python-multipart==0.0.6
pydantic==2.5.0
# Required for pydantic's EmailStr (used in schemas.py) - without this the
# app crashes on import at startup (ImportError: email-validator is not
# installed).
email-validator==2.1.1
python-dotenv==1.0.0
