fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
alembic==1.12.1
psycopg2-binary==2.9.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 doesn't pin bcrypt, so pip installs the latest bcrypt
# (4.1+), which raises ValueError on its own internal 72-byte self-test
# instead of the silent-truncate behavior passlib 1.7.4 was written
# against — crashes the FIRST hash/verify call ever made (i.e. the first
# register or login), which is why the container passed startup but died
# the moment a request hit /auth/register. Known passlib/bcrypt
# incompatibility; pinning bcrypt<4.1 is the standard fix.
bcrypt==4.0.1
python-multipart==0.0.6
pydantic==2.5.0
pydantic-settings==2.1.0
# Required for pydantic's EmailStr (used in app/api/routes/auth.py and
# app/schemas/auth.py) — without this, the app crashes on import at
# startup (ImportError: email-validator is not installed). Not previously
# listed here even though the code already depended on it.
email-validator==2.1.1
openai==1.3.0
python-dotenv==1.0.0
httpx==0.25.1
redis==5.0.1
# Used by app/api/routes/prevention.py to call OpenWeather/TomTom directly
# (sync calls, not via httpx) — missing entirely before, which crashed the
# whole app at import time (ModuleNotFoundError: No module named 'requests'),
# so mobile-api never actually started.
requests==2.32.3
